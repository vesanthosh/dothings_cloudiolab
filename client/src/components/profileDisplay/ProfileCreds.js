import React, { Component } from 'react';

class ProfileCreds extends Component {
    render() {
        return (
            <div>
                {/* TODO: If user wants to show his todo list then only we should show the result. otherwise we should display he is not sharing message. */}
            </div>
        );
    }
}

export default ProfileCreds;